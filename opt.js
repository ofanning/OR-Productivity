yt = function blockyt (link) {
    var current = window.location.href;
    if (current.includes(link)) {
        document.body.innerHTML = "";
        document.body.innerHTML = "Blocked LOL";
    }
}

yt = ("youtube.com");

fb = function blockfb (link) {
    var current = window.location.href;
    if (current.includes(link)) {
        document.body.innerHTML = "";
        document.body.innerHTML = "Blocked LOL";
    }
}

fb = ("facebook.com");

bf = function blockbf (link) {
    var current = window.location.href;
    if (current.includes(link)) {
        document.body.innerHTML = "";
        document.body.innerHTML = "Blocked LOL";
    }
}

bf = ("buzzfeed.com");

ig = function blockig (link) {
    var current = window.location.href;
    if (current.includes(link)) {
        document.body.innerHTML = "";
        document.body.innerHTML = "Blocked LOL";
    }
}

ig = ("instagram.com");